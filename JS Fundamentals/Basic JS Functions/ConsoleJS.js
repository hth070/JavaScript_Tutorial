let p = 234; //or = null adn then we increase it as below
let b = !!p; //we convert a variable to its boolean representataion, returns false
p++; // null gets converted to 0, then increased by 1
p+= 10; // here get increased by 10
console.log(b); 


//Automatic type conversion lside