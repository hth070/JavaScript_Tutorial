/* function lastMonth ([day, month, year]) {
let date=new Date(year, month-1, 0); //denotes previous month; 0 denotes max days from previous month 
let days=date.getDate(); //or .getMonth, last one
console.log(days);
}

lastMonth ([17, 3, 2002])  */

function test() {
    console.log("HELLO".repeat(10) + "+" + ":)".repeat(5))
}

test();