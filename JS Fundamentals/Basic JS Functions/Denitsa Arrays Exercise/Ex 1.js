let arr = [1, 2, 3];  // declaring an array that we ca fill with elements
/*let item = arr[arr.length-1];
//let item = arr.shift(); // shift is array METHOD that modifies it
//console.log(arr.join(' - '));

a string can become an array with split
Kak se obhohda array? - with a for cycle
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
reverse, splice, concat, etc. from METHODS on left side   -  learn how to use and explain
kak se obikalq array, kak se vyrti for cycle
all methods are implemented with a for cycle

if (arr.includes(5)) {
    doifjsksas...
} */

/* for (const element of arr) {
    console.log(element);     // This is the FOR - OF, but we do not have access to the index
}

for (let i = 0; i < arr.length; i++) {  // standard for cycle. obhojdame array from 0 to last
    const element = arr[i]; 
    console.log(element)
}        */


/*let str = "a,b,c".split(", ");
let item = arr.includes(4);
console.log(arr); 
console.log(item);
console.log(str);*/

let splited = 'stiin'.split('i');
console.log(splited)

trqbva da imam epromenliva, v koqto da se zapameti noviq masiv