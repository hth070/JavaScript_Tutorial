function calculateCircleAreaAndPerimeter(input1) {
    let r = input1; 
    console.log("Area  = " + Math.PI * r *r);
    console.log("Perimeter  = " + 2 * Math.PI * r);
}

calculateCircleAreaAndPerimeter(10);